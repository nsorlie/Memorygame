import java.util.ArrayList;
import java.util.Random;
import java.util.Scanner;
//import functions
public class memory{
//declares the class

    public void main(String[] args){
        //Declares the main function

         Scanner input = new Scanner(System.in) {
         //Starts scanner

        String[] Items = {"dog", "bucket", "window", "cup", "table",
         "cat", "frog", "bench", "track", "pencil", "fire", "water", 
         "house"};
        
         //String of Items to be randomly chossen from

        
        String answerOne = obj.getRandomElement(Items);
        String answerTwo = obj.getRandomElement(Items);
        String answerThree = obj.getRandomElement(Items);
        String answerFour = obj.getRandomElement(Items);
        String answerFive = obj.getRandomElement(Items)
        //Stores the 5 words for the user to remember in variables

        System.out.println("Remember thiese words:");
        //Tells the user what they are supposed to do

        System.out.println(answerOne);
        System.out.println(answerTwo);
        System.out.println(answerThree);
        System.out.println(answerFour);
        System.out.println(answerFive);
        
        //Prints out 5 different words for the user to remember


        //Add in something that clears whats been put on the screen


        Thread.sleep(5000)

        
        //Gets the User data about the words
        System.out.println(Type the first word);
        public String wordOne = input.nextString();
        System.out.println(Type the second word);
        Public String wordTwo = input.nextString();
        System.out.println(Type the third word);
        public String wordThree = input.nextString();
        System.out.println(Type the fourth word);
        Public String wordFour = input.nextString();
        System.out.println(Type the final word);
        Public String wordFive = input.nextString;


        public Boolean  checkOne = answerOne = wordOne
        public Boolean checkTwo = answerTwo = wordTwo
        public Boolean checkThree = answerThree = wordThree
        public Boolean checkFour = answerFour = wordFour
        public Boolean checkFive = answerFive =  wordFive
        if (checkOne = true){
         if (checkTwo = true){
         if (checkThree = true){
         if (checkFour = true){
         if (checkFive = true){
        System.out.println(Correct!);
        } else {

        System.out.println(Incorrect!);

        }
        } else {

        System.out.println(Incorrect!);

        }
        } else {

        System.out.println(Incorrect!);

        }
        } else {

        System.out.println(Incorrect!);

        }
        } else {

        System.out.println(Incorrect!);

        }
        




        }






    }
}
